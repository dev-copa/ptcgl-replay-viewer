<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>PTCGL Replay Viewer</title>
</head>
<body>

    <h1>Home page</h1>
    <form action="/logs/parse" method="post">
        <label>
            <textarea name="battleLogs" rows="30" cols="80" placeholder="Paste battle log here"></textarea>
        </label>
        <label>Select output format</label>
        <label>
            <select name="outputFormat">
                <option value="xml">XML</option>
                <option value="json">JSON</option>
            </select>
        </label>
        <button type="submit">Parse logs</button>
    </form>
</body>